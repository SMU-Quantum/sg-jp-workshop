# IBM Quantum Platform credentials.
# Keep the real .env file local and never commit it.
IBM_QUANTUM_TOKEN=replace-with-your-api-key
IBM_QUANTUM_INSTANCE=replace-with-your-instance-crn

# Optional: select a stable backend for the live demonstration.
# IBM_BACKEND=ibm_...

